# Render Blueprint — lets Render configure the service automatically.
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: jingzhong-ville
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
